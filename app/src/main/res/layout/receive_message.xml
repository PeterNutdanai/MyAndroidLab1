<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/receive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="Message goes here"
        android:textSize="18sp"
        app:layout_constraintStart_toEndOf="@+id/recieveImage"
        app:layout_constraintTop_toTopOf="@+id/recieveImage" />

    <TextView
        android:id="@+id/timeReceive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="time goes here"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@+id/receive"
        app:layout_constraintTop_toBottomOf="@+id/receive" />

    <ImageView
        android:id="@+id/recieveImage"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:srcCompat="@drawable/receive_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>